import React, {Component} from "react";
import "./MobileNavbar.css";

const MobileNavbar = props => (
	
	<nav className="mobile-Navbar">
		<img className="mobile-logo" src={"/assets/img/quizit-logo-white.png"} />
	</nav>
)

export default MobileNavbar;